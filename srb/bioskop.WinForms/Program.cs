
using System;
//using Gtk;
using System.Collections;
using System.Collections.Generic;
//using GLib;
using System.IO;
using Eto.Forms;
using Eto.Drawing;
using bioskop;
using Eto;



namespace Bioskop
{
	class MainClass
	{
		public static Korisnik ulogovani = null;

		public static void Main (string[] args)
		{
			//			testiranje ();

			//			object[] niz = {new object(), new object() };
			//			object preneseno = niz;
			//			object[] kastovano = (object[])preneseno;
			//
			//			Type tipKastovano = kastovano.GetType ();
			//			if (tipKastovano.IsArray) 
			//			{
			//				Console.WriteLine ("niz");
			//				Console.WriteLine (kastovano.Length);
			//			}
			//			else
			//				Console.WriteLine ("tip: " + tipKastovano);
			//	
			//
			//			Application.Init ();
			//			MainWindow m_win = new MainWindow ();
			//			m_win.Show ();
			//			Application.Run (); 
			//new Application ( Platforms.WinForms ).Run( new MainForm ( ) );

		}

	
	}
}
